#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Returns 0 (successful)
 */

int main(void)
{
char hpla;

for (hpla = 'z'; hpla >= 'a'; hpla--)
{
putchar(hpla);
}
putchar('\n');

return (0);
}

